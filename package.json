{
  "name": "bar-rush",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "prepare:customers": "node scripts/prepare-customers.mjs",
    "prepare:cyber-icons": "node scripts/prepare-cyber-icons.mjs",
    "prepare:cocktails": "node scripts/prepare-cocktail-icons.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
